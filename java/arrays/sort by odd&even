import java.util.*;

public class ce {
    
    public static void main(String[] args){
        Scanner input = new Scanner(System.in);
        int arrayImpar[] = new int[5];
        int arrayPar[] = new int[5];
        int array[] = new int[5];

        for(int i = 0; i < 5; i++){
            System.out.print("Enter a number: ");
            array[i] = input.nextInt();
        
            if(array[i] % 2 == 0){
                arrayPar[i] = array[i];

            }else{
                arrayImpar[i] = array[i];
            }
        }
            System.out.println("Even numbers: " + Arrays.toString(arrayPar));
            System.out.println("Odd numbers: " + Arrays.toString(arrayImpar));
            System.out.println("All numbers: " + Arrays.toString(array));
        
        }
    }
